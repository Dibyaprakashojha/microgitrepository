spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/training
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect

message: Have a great day - from production profile